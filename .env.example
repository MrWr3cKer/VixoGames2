# Copy to .env.local for local testing with `vercel dev` only.
# On production: set in Vercel Dashboard → Project → Settings → Environment Variables.
# NEVER commit the real webhook URL to git.

DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN
